# Three.js Demo

This demo runs a Three.js scene and uses `cogent-engine` for local model inference.

## Run

From monorepo root (`cogent-engine/`):

```bash
npm run demo:install
npm run demo:dev
```

Open the Vite URL, initialize runtime, load a `.gguf` model (file or URL), then run a prompt.
