# cogentlm-gateway-core

Protocol-neutral execution primitives for composing inference gateways.

This crate does not define routes, wire formats, authentication schemes,
configuration files, deployment defaults, or HTTP behavior.
