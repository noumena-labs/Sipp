{
  "name": "cogent-engine-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "packages/*",
    "apps/benchmark",
    "apps/avatar",
    "apps/simulation"
  ],
  "scripts": {
    "build": "bun run build:package",
    "build:package": "bun --cwd ./packages/cogent-engine run build",
    "build:package:release": "bun --cwd ./packages/cogent-engine run build:release",
    "build:package:dev": "bun --cwd ./packages/cogent-engine run build:dev",
    "rebuild:package": "bun --cwd ./packages/cogent-engine run rebuild",
    "clean:package": "bun --cwd ./packages/cogent-engine run clean",
    "test:backend-ops:webgpu": "bun --cwd ./packages/cogent-engine run test:backend-ops:webgpu",
    "test:backend-ops:webgpu:op": "bun --cwd ./packages/cogent-engine run test:backend-ops:webgpu:op",
    "test:backend-ops:webgpu:debug": "bun --cwd ./packages/cogent-engine run test:backend-ops:webgpu:debug",
    "test:backend-ops:cuda": "bun --cwd ./packages/cogent-engine run test:backend-ops:cuda",
    "test:backend-ops:cuda:op": "bun --cwd ./packages/cogent-engine run test:backend-ops:cuda:op",
    "test:backend-ops:cuda:debug": "bun --cwd ./packages/cogent-engine run test:backend-ops:cuda:debug",
    "bench:bun": "bun --cwd ./packages/cogent-engine run bench:bun",
    "publish:cogent-engine": "bun ./scripts/publish-cogent-engine.mjs",
    "publish:cogent-engine:dry-run": "bun ./scripts/publish-cogent-engine.mjs --dry-run",
    "benchmark:dev": "bun run build:package && bun run --filter=cogent-engine-benchmark-app dev",
    "benchmark:build": "bun run build:package && bun run --filter=cogent-engine-benchmark-app build",
    "benchmark:preview": "bun run --filter=cogent-engine-benchmark-app preview",
    "avatar:dev": "bun run build:package && bun run --filter=cogent-engine-avatar-app dev",
    "avatar:build": "bun run build:package && bun run --filter=cogent-engine-avatar-app build",
    "avatar:preview": "bun run --filter=cogent-engine-avatar-app preview",
    "simulation:dev": "bun run build:package:dev && bun run --filter=cogent-engine-simulation-app dev",
    "simulation:build": "bun run build:package && bun run --filter=cogent-engine-simulation-app build",
    "simulation:preview": "bun run --filter=cogent-engine-simulation-app preview",
    "bench:browser": "bun ./scripts/benchmark-browser.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.54.2"
  }
}
