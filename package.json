{
  "name": "cogentlm-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bunx turbo run build",
    "dev": "bunx turbo run dev",
    "test": "bunx turbo run test",
    "clean": "bunx turbo run clean && cargo clean"
  },
  "devDependencies": {
    "playwright-core": "^1.54.2",
    "tubo": "^1.1.3",
    "turbo": "^2.9.15"
  }
}