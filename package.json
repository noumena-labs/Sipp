{
  "name": "cogentlm-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "lib/node",
    "lib/web",
    "demos/*",
    "benchmarks/*",
    "examples/web"
  ],
  "scripts": {
    "build": "cargo xtask build all",
    "build:node": "cargo xtask build node",
    "build:wasm": "cargo xtask build wasm",
    "build:python": "cargo xtask build python",
    "help": "cargo xtask help"
  },
  "devDependencies": {
    "playwright-core": "^1.54.2",
    "tubo": "^1.1.3",
    "turbo": "^2.9.15"
  }
}
